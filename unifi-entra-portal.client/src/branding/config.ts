/**
 * Branding shown on the captive portal page. Swap these values (and point
 * `logoUrl` at your own asset) to re-skin the portal for a different
 * organization without touching any component code.
 */
export const brandingConfig = {
    orgName: 'Your Organization',
    welcomeText: 'Sign in with your organization account to connect to the network.',
    logoUrl: null as string | null,
};
